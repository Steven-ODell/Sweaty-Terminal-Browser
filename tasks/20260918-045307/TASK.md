# Create a window horizontally for typing and make paths cutoff at window width

- STATUS: IN_PROG 
- PRIORITY: 50
- TAGS:

This is half done. It still needs the typing window but the render window when things are drawn as rows cuts off. Just not the window sliding if you were a psycho and decided to type a folder longer than your terminal window.
